questionary>=1.10.0
names>=0.3.0
pygithub>=1.55
docker>=5.0.0
jinja2>=3.0.0
rich>=13.0.0
pyyaml>=6.0
requests>=2.28.0

# Security Notes:
# The following security features are implemented using Python's built-in libraries:
# - secrets (cryptographically secure random generation)
# - re (input validation with regex)
# - json (safe JSON parsing)
# - pathlib (secure path operations)
#
# All custom security validations are in: launchkit/utils/security_utils.py